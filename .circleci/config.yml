version: 2

jobs:
  tests:
    machine:
      enabled: true
    steps:
      - checkout
      - run: |
          make test -C tests || exit 1
  release:
    machine:
      enabled: true
    tag: /[0-9]+(\.[0-9]+)*/
    steps:
      - checkout
      - run: |
          echo $CIRCLE_TAG
          make release TAG=$CIRCLE_TAG || exit 1

workflows:
  version: 2
  test_and_release:
    jobs:
      - tests
      - release:
          requires:
            - tests
          filters:
            tags:
              only: /[0-9]+(\.[0-9]+)*/
